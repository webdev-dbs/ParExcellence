# Component scripts

Create one JavaScript module per Webflow component in this directory, then
import it from `index.js`. Prefer `data-*` attributes as JavaScript hooks so
scripts do not depend on presentational Webflow class names.
