# Component styles

Create one stylesheet per Webflow component in this directory, then import it
from `index.css`. Keep selectors scoped to the component to avoid leaking styles
into the rest of the Webflow site.
