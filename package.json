{
  "name": "parexcellence-webflow-assets",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Bundled CSS and JavaScript assets for the ParExcellence Webflow site.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "engines": {
    "node": ">=20"
  }
}
